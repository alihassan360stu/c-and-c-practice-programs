#include <iostream>
#include <conio.h>

using namespace std;

main()
{
      int a,b,c;
      cout<<"Enter the 1st no# = ";
      cin>>a;
      cout<<"Enter the 2nd no# = ";
      cin>>b;
      cout<<"Enter the 3rd no# = ";
      cin>>c;
      if(a>b)
          if(b>c)
             cout<<"A is the greatest";
                if(b>c)
                    if(c>a)
                       cout<<"B is hte greatest";
                          if(c>a)
                            if(a>b)
                              cout<<"C is ht greatest";
      getch();
}
