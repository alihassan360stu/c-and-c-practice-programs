#include <iostream>
#include <conio.h>

using namespace std;

main()
{
      char str[20];
      char token[20];
      cin>>str;
      char *p;
      p = str;
      *p++ = *p++ = 20;
      getch();
}
